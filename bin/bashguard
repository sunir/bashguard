#!/usr/bin/env bash
REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
VENV_PYTHON="${REPO_ROOT}/.venv/bin/python"
if [[ -x "$VENV_PYTHON" ]]; then
    exec "$VENV_PYTHON" -m bashguard.cli "$@"
else
    exec python3 -m bashguard.cli "$@"
fi
